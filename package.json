{
  "name": "test-basic-app-root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1",
    "typescript": "^4.0.2",
    "prettier": "^2.1.1"
  },
  "scripts": {
    "bootstrap": "npm i && lerna bootstrap",
    "docker": "docker-compose up",
    "format": "prettier",
    "watch": "tsc -b -w",
    "clean": "sh clean.sh",
    "clean-hard": "sh clean.sh --hard",
    "class-sample": "node samples/complete-sample/_/index.js"
  }
}
