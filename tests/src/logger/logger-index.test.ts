import { debugLogger } from "@microframework/logger"

xdescribe("validator > defaultValidator", () => {
  test("maxLength", () => {
    debugLogger({
      name: "testLogger",
      level: "verbose",
      type: "any",
      message: "asd",
    })
  })
})
