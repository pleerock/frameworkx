import { resolver } from "@microframework/core"
import { PostApp } from "../app";

/**
 * "postSave" mutation resolver.
 */
export const PostSaveResolver = resolver(PostApp, "postSave", () => {
  return {

  }
})
