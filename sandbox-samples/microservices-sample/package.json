{
  "name": "@microservices-test/root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1",
    "typescript": "4.2.2",
    "prettier": "^2.1.2"
  },
  "scripts": {
    "bootstrap": "npm i && lerna bootstrap",
    "format": "prettier",
    "watch": "tsc -b -w",
    "start": "docker-compose up"
  }
}
